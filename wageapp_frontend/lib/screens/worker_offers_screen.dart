import 'package:flutter/material.dart';

class WorkerOffersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Offers from Workers')),
      body: Center(child: Text('List of offers will be displayed here.')),
    );
  }
}
